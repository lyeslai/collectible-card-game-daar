import { Card } from './classes/card';
import { Set } from './classes/set';
import { IQuery } from './interfaces/query';
export declare class QueryBuilder {
    static all<T extends Card | Set>(type: {
        new (): T;
    }): Promise<T[]>;
    static find<T extends Card | Set>(type: {
        new (): T;
    }, id: string): Promise<T>;
    static where<T extends Card | Set>(type: {
        new (): T;
    }, params: IQuery[]): Promise<T[]>;
}
